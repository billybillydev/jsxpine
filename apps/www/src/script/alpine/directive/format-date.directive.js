/**
 * 
 * @param {HTMLElement} el 
 * @param {{ expression: string }} param1 
 */
export function dateFormatDirective(el, { expression }) {
  // let text = el.textContent.trim();
  // text = formatDateMagic()(text, expression, undefined, undefined, undefined);
  // el.textContent = text;
}
